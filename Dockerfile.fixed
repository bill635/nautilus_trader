# 修复版币安延迟测试
FROM python:3.11-slim

ENV PYTHONUNBUFFERED=1

RUN apt-get update && apt-get install -y gcc && apt-get clean

WORKDIR /app

RUN pip install nautilus-trader

COPY docker/fixed_latency_test.py ./

RUN mkdir -p reports

CMD ["python", "-u", "fixed_latency_test.py"]
