# WebSocket版币安延迟测试 - 无需API密钥
FROM python:3.11-slim

ENV PYTHONUNBUFFERED=1

WORKDIR /app

# 安装依赖
RUN pip install websockets

# 复制测试脚本
COPY docker/websocket_latency_test.py ./

# 创建报告目录
RUN mkdir -p /app/reports

# 运行测试
CMD ["python", "-u", "websocket_latency_test.py"]
