# CCXT轮询版币安延迟测试 Dockerfile
FROM python:3.11-slim

ENV PYTHONUNBUFFERED=1

WORKDIR /app

# 安装CCXT
RUN pip install --upgrade pip && \
    pip install ccxt

# 复制测试脚本
COPY binance_ccxt_polling_test.py ./

# 创建报告目录
RUN mkdir -p /app/reports

# 运行测试
CMD ["python", "-u", "binance_ccxt_polling_test.py"]
